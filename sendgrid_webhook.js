var colors = require('colors');
var localtunnel = require('localtunnel');

localtunnel(5000, { subdomain: 'jarjarsucks' }, function(err, tunnel) {
  console.log('The motha fucking local tunnel is running beotches!'.america)
});